# KeeperHub API key — generate at https://app.keeperhub.com/ (Setup Guide → Generate API Key)
KEEPERHUB_API_KEY=

# Workflow ID for the simplest possible scheduled workflow (block.number every 1 min).
# Set this AFTER you create the workflow in the visual builder (Step 2 of the README).
KEEPERHUB_WORKFLOW_ID=
