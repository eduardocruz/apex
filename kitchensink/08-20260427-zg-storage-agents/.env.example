# Throwaway wallet (same one used in /07 — copy from there or generate fresh).
# DO NOT use any wallet that holds real funds.
#   node -e "console.log('0x' + require('crypto').randomBytes(32).toString('hex'))"
PRIVATE_KEY=0x

# 0G Galileo Testnet (chainId 16602). Defaults applied if blank.
ZG_RPC_URL=https://evmrpc-testnet.0g.ai
ZG_INDEXER_URL=https://indexer-storage-testnet-turbo.0g.ai

# Optional. UI port. Default 5858.
PORT=5858
